- [ ] Remove MongoDB/Mongoose connection logic from server.js
- [ ] Remove mongoose models usage (models.js references) or replace with in-memory data only
- [ ] Ensure all API endpoints return in-memory data
- [ ] Remove/adjust package dependency if needed
- [ ] Run `node server.js` and verify `/api/health` and `/api/products` work

